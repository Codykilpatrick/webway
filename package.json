{
  "name": "webway-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for Redpanda cluster, binary data parser, and data generation utilities",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "yarn workspace @webway/data-generation run lint",
    "build": "yarn workspace @webway/data-generation run build",
    "test": "yarn workspace @webway/data-generation run test",
    "prettier:check": "prettier --check .",
    "prettier:fix": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "20.0.0",
    "@yarnpkg/sdks": "3.2.2",
    "prettier": "3.1.0",
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "yarn": ">=1.22.0"
  },
  "packageManager": "yarn@4.8.1"
}
